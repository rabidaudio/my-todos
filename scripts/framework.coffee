# Backbone = require 'backbone'
# Backbone.$ = window.jQuery
# Marionette = require 'backbone.marionette'

# This is kinda a bit ugly...

Parse = require('parse').Parse
Parse.$ = window.jQuery
# Parse.Marionette = Marionette

module.exports = Parse